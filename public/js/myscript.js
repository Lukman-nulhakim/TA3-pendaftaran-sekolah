const flashData = $('.flash-data').data('flashdata');

if (flashData) {
        Swal.fire(
            'Successfully !!',
            'Addition, Change Or Deletion Of Data !!',
            'success'
        );
}